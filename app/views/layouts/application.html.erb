<!DOCTYPE html>
<html>
  <head>
    <title>Everything Is Collateral </title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag "application", media: "all",
                                           "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= javascript_include_tag 'ckeditor/ckeditor.js' %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/partials/shim' %>
  </head>
  
  <script type="text/javascript">
  $(document).ready(function() {
    if ($('textarea').length > 0) {
        var data = $('textarea');
        $.each(data, function(i) {
            CKEDITOR.replace(data[i].id);
        });
    }
  });
  </script>

  <%= render 'layouts/partials/header' %>

  <body>
    <div class="container content">
    <!--Used to notify the user of errors-->
      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>

      <div id="login-window" style="display:none; overflow:hidden;">
        <%= render '/devise/sessions/newpopup' %>
      </div>
    
      <!--Yields all of the stuff you know and love.-->
      <%= yield %>
      <%= render 'layouts/partials/footer' %>
    </div>
  </body>
</html>
