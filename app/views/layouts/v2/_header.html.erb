<% if admin_signed_in? %>
  <div class="admin-banner">
    <%= link_to "Admin Dashboard", admin_path %>
    <%= link_to "Log Out", destroy_admin_session_path, method: :delete, class: "admin-action"%>
  </div>
<% end %>

<nav class="nav container">
  <div class="nav-logo-section">
    <%= link_to "Hi, I'm Taylor Jones", root_path, class: "nav-logo" %>
  </div>
  <div class="nav-spacer"></div>
  <div class="nav-menu">
    <button class="hamburger" data-action="sidebar#toggle" aria-label="Toggle menu">
      <span class="hamburger-line"></span>
      <span class="hamburger-line"></span>
      <span class="hamburger-line"></span>
    </button>
  </div>
</nav>
