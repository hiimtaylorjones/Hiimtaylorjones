
<div class="page">
	<h2 class="text-center"><%= @page.title %></h2>
	<%= markdown @page.content %>
</div>

<div class="bottom-nav text-center">
	<% if admin_signed_in? %>
		| <button type="button" class="btn btn-default"><%= link_to 'Edit Page', edit_page_path(@page.title) %></button>
	<% end %>
</div>