<div class="post">
	<h2><%= @post.title %></h2>
	<%= image_tag(@post.img) %>
	<p><%= simple_format @post.text %></p>
	<br/>
</div>


<%= link_to 'Back', posts_path %>
<% if can? :update, @post %>
	| <%= link_to 'Edit', edit_post_path(@post) %>
<% end %>