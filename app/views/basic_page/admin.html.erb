<div class="admin-posts-index">
  <h1>Listing Posts</h1>
  <table class="table table-striped is-fullwidth">
    <tr>
      <th>Title</th>
      <th colspan="3">Actions</th>
    </tr>
    <% @posts.each do |post| %>
      <tr>
        <td>
          <%= post.title %>
        </td>
        <td>
          <%= link_to 'Edit', edit_post_path(post), :class => "button is-warning" %>
        </td>
        <td>
          <%= button_to 'Destroy', post_path(post), :class => "button is-danger", method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </table>

  <div class="has-text-centered">
      <%= link_to "New Post", new_post_path, :class => "button is-primary" %>
    </div>
</div>

<div class="posts-index">
  <h1>Listing Pages</h1>
  <table class="table table-striped is-fullwidth">
    <tr>
      <th>Title</th>
      <th colspan="3">Actions</th>
    </tr>
    <% @pages.each do |page| %>
      <tr>
        <td>
          <%= page.title %>
        </td>
        <td>
          <%= link_to 'Edit', edit_page_path(page.title), :class => "button is-warning" %>
        </td>
        <td>
          <%= button_to 'Destroy', page_path(page.title), :class => "button is-danger", method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </table>

  <div class="has-text-centered">
      <%= link_to "New Page", new_page_path, :class => "button is-primary" %>
    </div>
</div>